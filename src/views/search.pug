extends base
include mixins/video

block content 
  form(method="get").search__form
    input(type='text' placeholder='Search by Id' name='keyword')
    button
      i.fas.fa-search
  div.video-grid
    each video in videos 
      +video(video)
    else 
      span.empty__message No video found